{
  "name": "tiny-helpdesk-hero",
  "version": "0.0.1",
  "description": "Empathy-forward comedy game for the LittleJS Game Jam 2025.",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "lint": "npm run validate:content",
    "format": "echo \"TODO: configure formatter\"",
    "test": "node --test tests",
    "build": "node scripts/build.mjs",
    "test:watch": "node --test --watch tests",
    "validate:content": "node scripts/validate-content.mjs",
    "report:monitor": "node scripts/report-monitor-readability.mjs",
    "analyze:gltf": "node scripts/analyze-gltf-bounds.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
