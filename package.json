{
  "name": "tiny-helpdesk-hero",
  "version": "0.0.1",
  "description": "Empathy-forward comedy game for the LittleJS Game Jam 2025.",
  "type": "module",
  "scripts": {
    "serve": "npx http-server -c-1 public",
    "lint": "echo \"TODO: configure linting\"",
    "format": "echo \"TODO: configure formatter\"",
    "test": "node tests/smoke.test.mjs",
    "test:watch": "node --watch tests/smoke.test.mjs"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
